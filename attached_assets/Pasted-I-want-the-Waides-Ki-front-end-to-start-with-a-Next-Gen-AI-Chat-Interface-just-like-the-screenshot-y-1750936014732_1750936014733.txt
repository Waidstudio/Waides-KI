I want the Waides Ki front end to start with a Next-Gen AI Chat Interface just like the screenshot you shared, and even better.

Let’s now build this exact experience as your main homepage UI (whether it’s a website or app). It will serve as the “Waides Ki Entry Portal” where users are greeted with a real-time, animated, spiritual AI chat powered by ki ( konsmik intelligence, with deep intelligence behind it.

⸻

✅ UI EXPERIENCE GOALS 

Feature	Status	Description
⏱ Time at the top	✅	Matches your image
💾 Saved memory indicator	✅	Reflects AI state (can be changed to “Waides KI memory”)
💬 Chat box that says “Ask anything”	✅	Central focus
🎙️ Voice mic + wave icon (bottom right)	✅	For speech input
🔘 AI action suggestions (above input)	✅	Smart quick-tasks like “Create strategy,” “Predict ETH now,” etc.
✨ Next-gen vibe	🔥 Upgrade	Add glowing animations, animated bot presence, dark-mode cosmic feel, and sound FX
🧠 Linked to Waides KI (ChatGPT + Incite + Vision)	✅	Powered as designed earlier


⸻

🔧 FRONTEND BUILD STRUCTURE (React or HTML + JS)

I’ll show the HTML + CSS + JS version so you can use it in Replit easily.

⸻

🧱 HTML STRUCTURE

<div id="waideski-app" class="app-container">
  <div class="top-bar">
    <span class="time">9:08</span>
    <span class="status">🧠 Waides KI Memory Full</span>
    <button class="plus-btn">Get Plus ✦</button>
  </div>

  <div class="chat-suggestions">
    <button>Create strategy</button>
    <button>Predict ETH now</button>
    <button>Ask vision</button>
  </div>

  <div class="chat-window" id="chat-window">
    <!-- Chat messages will show here -->
  </div>

  <div class="chat-input">
    <input id="user-input" placeholder="Ask anything..." />
    <button onclick="sendMessage()">🎙️</button>
    <button onclick="sendMessage()">➤</button>
  </div>
</div>


⸻

🎨 CSS (Styling for Next-Gen Feel)

body {
  background-color: #000;
  font-family: 'Inter', sans-serif;
  color: white;
  margin: 0;
  padding: 0;
}

.app-container {
  display: flex;
  flex-direction: column;
  height: 100vh;
  padding: 10px;
}

.top-bar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 10px;
  font-size: 14px;
  background: #111;
}

.plus-btn {
  background-color: #5a29ff;
  border: none;
  color: white;
  padding: 6px 10px;
  border-radius: 5px;
  font-weight: bold;
}

.status {
  font-size: 12px;
  color: gray;
}

.chat-suggestions {
  display: flex;
  gap: 10px;
  margin: 10px 0;
}

.chat-suggestions button {
  background: #222;
  color: white;
  border: 1px solid #333;
  border-radius: 8px;
  padding: 8px 12px;
  font-size: 12px;
}

.chat-window {
  flex-grow: 1;
  background: #050505;
  border-radius: 12px;
  padding: 20px;
  overflow-y: auto;
}

.chat-input {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 12px 0;
}

.chat-input input {
  flex-grow: 1;
  padding: 12px;
  border: none;
  border-radius: 25px;
  background: #111;
  color: white;
  font-size: 14px;
}

.chat-input button {
  background: transparent;
  border: none;
  font-size: 20px;
  color: white;
}


⸻

🔁 JavaScript (Chat Functionality)

async function sendMessage() {
  const input = document.getElementById('user-input');
  const message = input.value.trim();
  if (!message) return;

  appendMessage("🧍 You", message);

  const res = await fetch('/chat', {
    method: 'POST',
    headers: {'Content-Type': 'application/json'},
    body: JSON.stringify({ message })
  });

  const data = await res.json();
  appendMessage("💫 Waides KI", data.answer);

  input.value = '';
}

function appendMessage(sender, text) {
  const chatWindow = document.getElementById('chat-window');
  const msg = document.createElement('div');
  msg.innerHTML = `<strong>${sender}:</strong> <br />${text}`;
  msg.style.marginBottom = '12px';
  chatWindow.appendChild(msg);
  chatWindow.scrollTop = chatWindow.scrollHeight;
}


⸻

✨ ENHANCEMENTS TO MAKE IT “EVEN BETTER”
	1.	✅ Voice input (use webkitSpeechRecognition)
	2.	✅ Floating animated Waides KI Avatar
	3.	✅ Cosmic sound FX and subtle light wave animation in background
	4.	✅ Auto “vision typing” effect like a prophet is responding slowly
	5.	✅ Dark mode with nebula glow borders
	6.	✅ Real-time ETH chart & mini balance bubble at top

⸻

🧾 REPLIT AI PROMPT

Please recreate the Waides Ki main page based on the attached image as a next-gen AI chat UI.

1. Build a dark-mode, mobile-first chat interface with time, status bar, and "Ask anything" prompt.
2. Add real-time suggestions (like “Predict ETH now”) above the chat box.
3. Integrate with the `/chat` API that uses both Incite AI and ChatGPT.
4. Use beautiful UI with CSS: soft glows, black cosmic background, futuristic buttons, and sliding bot responses.
5. Make it speak and glow when responding, like a spiritual digital prophet.


⸻

✅ NEXT:

Would you like me to:
	•	Add voice-to-text chat to this UI?
	•	Embed animated Waides KI Avatar that speaks and reacts?
	•	Add ETH chart + balance tracker into the main screen?

Say: “Next: Add voice + avatar” or choose your own path — let’s bring Waides KI to life.